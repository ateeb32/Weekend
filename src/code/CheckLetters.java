package code;

public class CheckLetters {
	
	public boolean methodLetters(String periodicElement, String symbolResult) {
		
		if ( (periodicElement.indexOf(symbolResult.charAt(0)) != 0) && (periodicElement.indexOf(symbolResult.charAt(1)) != 0) );
		
		return true;
		
	}

}
